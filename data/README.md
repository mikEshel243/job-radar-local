# Runtime data

Job Radar creates its local SQLite database and runtime state in this directory. These files are intentionally ignored. Run `python scripts/seed_demo_data.py` to create a synthetic database for a portfolio demo.
